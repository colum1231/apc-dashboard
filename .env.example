# Client-safe. Exposed to the browser.
NEXT_PUBLIC_SUPABASE_URL=https://ujlnwgkdpwitscyxttmf.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# SERVER ONLY. Never prefix with NEXT_PUBLIC. This key bypasses all RLS.
# Set it in Vercel as an encrypted env var and never import it into a client component.
SUPABASE_SERVICE_ROLE_KEY=
